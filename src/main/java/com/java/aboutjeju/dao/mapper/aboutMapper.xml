<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.java.aboutjeju.dao.mapper.aboutMapper">

	<insert id="LMinsert" parameterType="com.java.aboutjeju.dto.AboutjejuDto">
		INSERT INTO Landmark VALUES(Landmark_LMnumber_seq.nextval,#{LMcategory},#{LMtitle},#{LMcontent},#{LMaddress},#{LMpostalcode},#{LMlatitude},#{LMlongitude},#{Iname},#{Isize},#{Ipath})
	</insert>
	
	<!-- <select id="IRead" parameterType="String" resultMap="aboutjejuDto">
		SELECT * from landmark where #{LMcategory} ='introduction'
	</select>
	
	<select id="LRead" parameterType="String" resultMap="aboutjejuDto">
		SELECT * from landmark where #{LMcategory} ='location's
	</select>
	
	<select id="SRead" parameterType="String" resultMap="aboutjejuDto">
		SELECT * from landmark where #{LMcategory} ='speciality'
	</select>
	
	<select id="ARead" parameterType="String" resultMap="aboutjejuDto">
		SELECT * from landmark where #{LMcategory} ='activites'
	</select> -->
	
	<!-- <select id="fullRead" parameterType="Iname" resultMap="aboutjejuDto">
		SELECT * FROM Landmark WHERE LMnumber=#{LMnumber} AND 
	</select> -->
	
	
</mapper>